import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://carrot-softwares.github.io',
    base: 'CarrotSoftwaresWebsite.github.io',
});
